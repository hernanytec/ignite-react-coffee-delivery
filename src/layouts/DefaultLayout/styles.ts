import styled from 'styled-components'

export const LayoutContaier = styled.div`
  max-width: 1440px;
  padding: 0 160px;
  margin: 0 auto;
  background-color: ${(props) => props.theme.colors.background};
`
